#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    vector<int>v(n);
    double sum=0;
    for(int i=0;i<n;i++)
    { 
      cin>>v[i];
    }
    for(int i=0;i<n;i++)
    {
        sum+=v[i];
    }
     double x=sum/n;
    cout<<setprecision(20)<<x;
    return 0;
}
