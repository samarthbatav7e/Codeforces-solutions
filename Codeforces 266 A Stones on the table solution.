#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin>>n;
    string s;
    cin>>s;
    n=s.length();
    int t=0;
    for(int i=0;i<n;i++)
    {
        if(s[i]==s[i+1])
        t++;
    }
    cout<<t;
    return 0;
}
